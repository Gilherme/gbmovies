import styles from "./Header.module.css";
function Header(){
  return(
    <header className={styles.header}>
      <span>GB Movies</span>
      <nav >
      
      </nav>
    </header>
  );
}

export default Header;
